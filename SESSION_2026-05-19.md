# 세션 2026-05-19 — air_watcher (대한항공 모니터링) 구현

## 진행 한 일

### 1) KTX/SRT 측 (오전)
- `srt 5월 22일 서울 부산 22시~23시 예매` 요청.
- 서울은 SRT 미정차 → **수서→부산** 으로 재선택.
- `.env.srt` 갱신, SRT 워처 백그라운드 기동. 22:00/22:40 둘 다 일반실 매진, 무한 재시도 모드로 폴링 중.
- `.env.ktx` / `.env.srt` gitignore 에서 제외, `Track .env.ktx/.env.srt journey files; keep .env (secrets) ignored` 커밋·푸시.

### 2) air_watcher 신규 (오후)
요청: 대한항공 CJU→GMP 2026-05-29 13:05 모니터링.

워처 골격 (`air_watcher/`, `.claude/skills/air/`, `.env.air`) 은 어제 만들어둔 상태. 이번 세션은 **로그인 + 위젯 워밍업 + air-bounds API 폴링** 통합 작업.

### 3) 로그인 (Playwright) — ✅ 작동
- `reserve.py:ensure_logged_in` 구현.
- 위젯 ID/PW 입력은 shadow DOM piercing IIFE + native setter 로.
- 비밀번호 변경 안내 모달 자동 dismiss (`90일 후에 변경` 클릭).
- 로그아웃 indicator hydration 8초 polling.
- 결과: 로그인 성공·세션 쿠키 적재 OK.

### 4) air-bounds API 폴링 (XHR) — ❌ 403
- `search.py` 를 어제 검증된 `runs/cdp_search_flights.py` 의 air-bounds POST 패턴으로 교체.
- fetch → XHR + 최소 헤더 (Accept/Content-Type) 로 KE 자체 XHR 트래픽과 동일하게 맞춤.
- 그래도 항상 **Akamai 403 Access Denied**.

### 5) 원인 발견
- `goto("/booking/select-flight/...")` 후 `page.url` 확인 → `https://www.koreanair.com/` 로 redirect 되어 있음.
- Akamai 가 booking 페이지 **직접 진입을 차단**, 위젯 클릭 경로로만 합법 진입 가능.
- Gemini 교차 검증: 마우스/스크롤·헤더 위조·다른 경로 경유·Playwright stealth 모두 "거의 불가". **위젯 자동화가 유일한 안정 경로**.

### 6) 위젯 warm-up 작업 시작 (`reserve.py:warm_up_select_flight`)
- 홈 진입 → 편도 chip → CJU 출발지 → GMP 도착지 → 2026-05-29 날짜 → 항공편 검색.
- 첫 시도: trip chip 활성화 실패 (radio input 자체 `.click()` 은 Angular 가 안 받음).
- 수정: `label[for=chip-X]` 클릭으로 활성화 OK.
- 다음: airport picker 가 열리지 않음 — `_widget_button` 이 "출발지와 도착지 바꾸기" swap button 을 anchor 로 찾는데 main DOM 에선 못 찾음.
- 화면 캡쳐 + shadow DOM piercing 으로 진짜 구조 파악:
  - 위젯이 전부 **shadow DOM (`KDS-DIALOG` 커스텀 element)** 기반.
  - 새 button 좌표 확보 (shadow-piercing):
    - `ui-fromto__button -order1` (x=277) `출발지 CJU 제주`
    - `ui-fromto__button -order3` (x=474) `도착지 GMP 서울/김포`
    - `ui-booking-tool__button` (x=658) `출발일 가는 날`
    - 항공편 검색 (x=1522)
  - CJU/GMP 가 자동 적용된 상태 (이전 시도의 잔여 또는 picker 추천카드 클릭 결과).
- 출발일 button 클릭 → date picker (`ui-datepicker__dialog -is-visible`) 열림 확인.
- 5월 29일 셀 클릭 — TD getBoundingClientRect 이 0,0 반환 (shadow DOM 안의 layout coord 가 외부에서 안 보임). 클릭 mouse event 는 발사됐고 클래스 `-start -end` 가 셀에 박힘 → 선택은 됐을 가능성.
- 날짜 button text 갱신 미확인 ("출발일 가는 날" 그대로) — 마지막 click 결과 검증 미완.

## 현재 상태

### 작동
- KTX, SRT 워처: 백그라운드에서 무한 재시도 중.
- air_watcher 로그인: 자동화 완료.

### 미완성
- air_watcher warm-up: trip chip + 출발/도착 (자동 반영분) 까지. 날짜 click 의 실제 반영 검증 못 함.
- 항공편 검색 button click → select-flight 진입 검증 미완.
- 진입 후 air-bounds 폴링 200 확인 미완.

## 다음 진행 시 우선순위
1. 날짜 click 실제 반영 확인 (button text 갱신, shadow DOM 안의 표시 확인).
2. 항공편 검색 click → URL 이 `/booking/select-flight/...` 로 진입하는지.
3. air-bounds XHR 가 200 으로 돌아오는지.
4. 그 다음 응답 파싱 정상화 + 시간 윈도우 필터.

## 코드 변경
- `air_watcher/koreanair/reserve.py`: `ensure_logged_in` 구현 + `warm_up_select_flight` 위젯 클릭 로직 + 비밀번호 변경 모달 dismiss + 비번 변경 모달 / 로그인 indicator hydration wait.
- `air_watcher/koreanair/search.py`: air-bounds API XHR 호출 패턴 + select-flight referer 경유.
- `air_watcher/main.py`: 로그인 + warm-up 호출, 워처 메인 루프 무한 재시도.
- `.env.air`: CJU→GMP 2026-05-29 13:05 economy 1명.

## 미해결 문제
- KE 위젯이 shadow DOM 기반이라 selector 작업이 매번 복잡. cdp_book_final.py 의 raw CDP 패턴을 Playwright 로 옮기는 중인데 매 단계 진단·캡쳐 필요.
- 결국 사용자가 한 번 손으로 select-flight 진입 → 워처가 그 다음 폴링만 하는 운영 모드가 더 빠를 수 있음 (현재 코드의 `_ensure_select_flight_referer` 가 drift 감지 시 warm-up 재호출하므로, warm-up 본문을 NotImplementedError 로 두면 됨).
