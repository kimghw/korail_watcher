[project]
name = "binjari"
version = "0.1.0"
description = "빈자리(binjari) — KTX/SRT/대한항공 빈자리 감시·예매 워처"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "platformdirs>=4.5.1",
    "playwright==1.55",
    "playwright-stealth>=2.0.2",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "python-telegram-bot>=22.5",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
    "rich>=14.2.0",
    "tenacity>=9.1.2",
    "tzdata>=2025.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-mock>=3.15.1",
]
