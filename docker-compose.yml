version: "3.9"

services:
  srt-watcher:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    environment:
      - SRT_CDP_URL=http://host.docker.internal:9222
    volumes:
      - ./runs:/app/runs
    # restart: unless-stopped
    